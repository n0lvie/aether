[package]
name = "aether-ebpf-common"
version = "0.1.0"
edition = "2021"
description = "Shared types between kernel-space eBPF programs and user-space control plane"

[features]
default = []
user = ["aya"]

[dependencies]
aya = { version = ">=0.12", optional = true }

[lib]
path = "src/lib.rs"
